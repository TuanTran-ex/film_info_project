class Category {
  constructor(id, name, updateAt, createdAt) {
    this.id = id;
    this.name = name;
    this.updateAt = updateAt;
    this.createdAt = createdAt;
  }
}

module.exports = Category;
